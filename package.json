{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --scope={server-api,web-client}",
    "test": "lerna run test --scope={web-client,desktop-client,server-api,main-app}",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
